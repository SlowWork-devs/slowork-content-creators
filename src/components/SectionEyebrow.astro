---
interface Props {
  num: number;
  text: string;
}

const { num, text } = Astro.props;
---

<span
  class="mb-4 block font-mono text-[0.65rem] font-semibold tracking-widest text-primary/40 uppercase"
>
  {String(num).padStart(2, '0')} / {text}
</span>
